<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:angle="270"
                android:centerColor="@color/progressbar"
                android:centerY="1.0"
                android:endColor="@color/progressbar"
                android:startColor="@color/progressbar" />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/colorPoint"
                    android:centerY="1.0"
                    android:endColor="@color/colorPoint"
                    android:startColor="@color/colorPoint" />
            </shape>
        </clip>
    </item>


</layer-list>
